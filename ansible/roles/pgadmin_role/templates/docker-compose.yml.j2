version: '3.8'

services:
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin4_container
    ports:
      - "8888:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: matickeny@gmail.com
      PGADMIN_DEFAULT_PASSWORD: reins10blader
    volumes:
      - pgadmin-data:/var/lib/pgadmin

volumes:
  pgadmin-data:

networks:
  default:
    name: pgadmin_network
